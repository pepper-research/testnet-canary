use spicenet_stf::runtime::RuntimeSubcommand;
use spicenet_rollup::celestia_rollup::CelestiaRollup;
use sov_modules_api::cli::{FileNameArg, JsonStringArg};
use sov_modules_rollup_blueprint::WalletBlueprint;

include!("../../chain_hash.autogenerated.rs");

#[tokio::main]
async fn main() -> anyhow::Result<()> {
    CelestiaRollup::run_wallet::<
        RuntimeSubcommand<FileNameArg, _>,
        RuntimeSubcommand<JsonStringArg, _>,
    >()
    .await
}